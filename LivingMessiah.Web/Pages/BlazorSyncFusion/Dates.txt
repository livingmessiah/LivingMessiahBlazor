

# DisabledDates.razor; SfDatePicker

> All the weekends of a month are disabled, and these dates are restricted to set or select in the **DatePicker**
> This is done via code using hte OnRenderDayCell event which ets triggered on each day cell element creation,

### Highlights
- @ref, OnRenderDayCell="@DisableDate"

- C:\Users\JohnM\source\repos\syncfusion\blazor-samples\Pages\Calendars\DatePicker\DisabledDates.razor

```html
<div class="control-section">
  <div id="wrapper" class="datepicker-section">
    <div id="datepicker-control">

        <SfDatePicker TValue="DateTime?" @ref="@DatePickerObj" Placeholder="Choose a Date">
            <DatePickerEvents TValue="DateTime?" OnRenderDayCell="@DisableDate"></DatePickerEvents>
        </SfDatePicker>

    </div>
  </div>
</div>
```

```csharp
@code {
    private SfDatePicker<DateTime?> DatePickerObj { get; set; }

    private void DisableDate(RenderDayCellEventArgs args)
    {
        var view = DatePickerObj.CurrentView();
        if (view == "Month" && ((int)args.Date.DayOfWeek == 0 || (int)args.Date.DayOfWeek == 6))
        {
            args.IsDisabled = true;
        }
    }
}
```


# FormsValidation.razor
